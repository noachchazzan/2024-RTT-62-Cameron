package JavaMadeMeDoIt;

public class JavaSort {
    /*
    import java.io.*;
import java.util.*;

public class Solution {

    public static void main(String[] args) {

    Scanner cin = new Scanner(System.in);
    List<String> list = new ArrayList<String>();

    int n = cin.nextInt();
        cin.nextLine();

        for (int i = 0; i < n; i++){
        String entry = cin.nextLine();
        list.add(entry);
    }

    // Sorting logic
        Collections.sort(list, new Comparator<String>(){
        @Override
        public int compare(String o1, String o2){
            String[] parts1 = o1.split(" ");
            String[] parts2 = o2.split(" ");
            double gpa1 = Double.parseDouble(parts1[2]);
            double gpa2 = Double.parseDouble(parts2[2]);
            if(gpa1 != gpa2) {
                return Double.compare(gpa2, gpa1); // sort by GPA descedning
            } else {
                return parts1[1].compareTo(parts2[1]);// sort by name ascending
            }
        }
    });

    for (String student : list) {
        System.out.println(student.split(" ")[1]); // print the first name of each student
    }
}
}
     */
}
